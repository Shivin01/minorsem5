
  <center>
  <div class="box2">
    <!--<div class="power">☮</div> -->
    <div class="userbox">
       <%= image_tag current_user.thumbnail.url(:medium) %>
      <div class="pic">
        <!-- <% image_tag current_user.thumbnail.url(:medium) %> -->
      </div>
    </div>
    <p>Hi, <span class="user1"> 
    
    
        <strong>
          <%= current_user.name %>
        </strong>
      
</div>
</span></p>
</div>
<%= link_to "Search People", users_path, :class => "my-class" %>
<%= link_to "News Feed", activities_path(:user_id), :class => "my-class2" %>
<div class = "box3">
<div class="transitions-enabled" id="pins">
  <% @pins.each do |pin| %>
    <div class="box panel panel-default">
      <%= link_to (image_tag pin.image.url), pin %>
      <div class="panel-body">
        <h2>
          <%= link_to pin.title, pin %>
        </h2>
      </div>
    </div>
  <% end %>
</div>

</div>

</center>


